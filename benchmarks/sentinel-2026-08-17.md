# Sentinel qualification run

Date: 2026-08-17

Commit under test: `1d6ba08`.

## Host

- Linux `sentinel`, Ubuntu 24.04.4 LTS, kernel `6.8.0-137-generic`, x86_64;
- Go `go1.25.13 linux/amd64`, pinned by the `toolchain` directive in `go.mod`;
- Intel Core i7-1165G7.

## Results

| Gate | Command | Result |
|---|---|---|
| Phase 4 | `scripts/phase4-sentinel-gate.sh` | `phase4-gate PASS` |
| Phase 6 | `scripts/phase6-sentinel-gate.sh` | `phase6-sentinel-gate PASS` |
| Phase 5 cluster | run inside the Phase 6 gate | `phase5-sentinel-cluster PASS` |
| Environment report | `scripts/sentinel-report.sh` | pass |
| Bounded TLC | `promtact verify` | complete, no violation |
| Durable file writes | `promtact uring-bench` | 2,189 ops/s |
| Durable raw NVMe writes | `promtact raw-bench` | 7,971 ops/s |

The Phase 6 gate wrote `gate.log` and `SHA256SUMS` to
`benchmarks/artifacts/sentinel-phase6-20260817T152258Z/` on the qualification
host.

## Race suite and repetition

The Phase 4 gate runs the complete suite under the race detector, then repeats
the consensus and storage packages a hundred times each. `sim` accounts for
495.49 s of that.

## Linearizability

The live five-node workload used Jepsen with the Knossos register checker.
During the measured history the harness killed the leader process and applied
network loss to an isolated node. The final result was:

```clojure
{:linearizable {:valid? true}
 :timeline {:valid? true}
 :valid? true}
```

Recorded at
`jepsen/store/promtact-live-linearizability/20260817T172938.145+0200/results.edn`.

## Bounded model checking

TLC explored the configured finite model:

| Bound or result | Value |
|---|---:|
| Nodes | 3 |
| Maximum term | 2 |
| Maximum log length | 2 |
| States generated | 46,667,923 |
| Distinct states | 6,121,927 |
| State-graph depth | 25 |
| States remaining | 0 |
| Invariant violations | 0 |
| Duration | 02min 40s |

Modeled transitions include durable election, AppendEntries replication,
current-term commit, compaction, InstallSnapshot, joint and final membership,
and crash recovery. Checked invariants cover type correctness, election safety,
committed-prefix safety, snapshot safety, and durable-vote safety.

Fingerprint-collision estimates were `1.3E-5` optimistic and `8.3E-7` from the
actual fingerprints. The second figure is a property of the run, since TLC draws
a fresh fingerprint seed each time.

Under `-workers auto` TLC can report one level more than the graph depth,
because a worker may reach the next level before the current one closes. A
serial run is the reliable reading.

This is bounded model checking, not an unbounded proof.

## Kernel data path

```
platform=linux/amd64 go=go1.25.13 entries=8
io_uring_setup: PASS
```

## Deterministic sweep

```
PASS seeds=1000 range=1..1000 steps=1000
```

## Measurements

Durable 4096-byte writes through registered `io_uring` against a file on ext4
over `/dev/sda2`, one completed `FSYNC` per operation:

| Operations | Throughput | p50 | p99 | Maximum |
|---:|---:|---:|---:|---:|
| 1,000 | 2,189 ops/s | 449.616 us | 543.602 us | 1.517617 ms |

Durable 4096-byte writes to the raw NVMe device `/dev/nvme0n1`, 20 GiB, blocks
256 through 10255, registered file and buffer, `O_DIRECT`, `WRITE_FIXED`, CQE
validation, and one completed `FSYNC` per operation:

| Operations | Throughput | p50 | p99 | Maximum |
|---:|---:|---:|---:|---:|
| 10,000 | 7,971 ops/s | 117.207 us | 196.343 us | 13.668893 ms |

WAL record encoding:

```
BenchmarkEncode-4   56776249   23.34 ns/op   0 B/op   0 allocs/op
```

## Claim boundary

These are results for this host and these configurations: a file on a
SATA-backed ext4 filesystem, and the 20 GiB NVMe device in this machine. The
linearizability result applies to the recorded workload. The formal result
applies to the documented finite bounds. The zero-allocation figure applies to
the measured encode path.
